# pytest-asyncio is configured globally in pyproject.toml (asyncio_mode = "auto").
# This file makes tests/v2/ a recognized pytest package sub-directory and will
# hold v2-specific fixtures as they are added in later tasks.
